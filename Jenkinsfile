pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Replace this with your build commands
                echo 'Building your project...'
            }
        }
    }
}
